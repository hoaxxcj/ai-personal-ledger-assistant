```markdown
# AI 个人记账与消费分析助手
## 产品需求文档 (PRD) — 本地 LLM 驱动的智能财务管家

| 文档版本 | 修改日期   | 修改人 | 修改说明 |
| -------- | ---------- | ------ | -------- |
| V1.0     | 2026-05-20 | 实训小组 | 全面更新为 DeepSeek API 版本 |

**文档定位**：本 PRD 为 AI 编程实训营 Module 1 的核心实践文档，展示如何为“AI 工具”类产品编写结构化需求。与“AI 审稿人”SaaS 平台不同，本文档聚焦于**本地数据存储 + 云端大模型**的个人效率工具，强调提示词工程、智能体工作流、前端数据持久化与 API 集成。

**阅读建议**：PM 关注 1-3 节（场景与功能），架构师/开发者关注 4-6 节（架构、数据、非功能），所有成员应重点关注第 9 节（风险与 AI 编码实践）。

---

## 1. 产品概述

### 1.1 一句话定义
AI 个人记账与消费分析助手是一款**前端完全本地化、AI 分析调用 DeepSeek 云端的智能记账应用**。用户通过简单的收支记录，即可获得 AI 驱动的可视化报表、自然语言消费洞察与个性化省钱建议；完整流水存储在浏览器本地，仅匿名摘要上传至 DeepSeek，兼顾智能与隐私。

### 1.2 要解决的问题

| 现状痛点 | 本产品的解法 |
|----------|--------------|
| 记账应用仅做冰冷统计，缺乏解读 | DeepSeek 大模型自动生成口语化分析报告，指出异常 |
| 用户不知道自己“钱花哪儿了” | 类别占比、趋势图 + AI 对比健康财务比例 |
| 想省钱却无从下手 | AI 结合个人流水给出具体可执行的省钱策略 |
| 担心财务数据上云泄露隐私 | 完整流水本地存储，仅上传匿名统计摘要；DeepSeek 承诺不保留用户请求数据 |
| 现有记账软件学习成本高 | 极简界面，AI 辅助归类，上手零门槛 |

### 1.3 目标用户

| 用户角色 | 核心诉求 | 使用场景 |
|----------|----------|----------|
| 大学生/职场新人 | 控制生活开支，了解消费结构 | 每日随手记账，月底看分析 |
| 自由职业者 | 区分工作与生活支出，简单财务管理 | 记录多类收支，季度回顾 |
| 对隐私敏感人群 | 云端智能与本地隐私的平衡方案 | 替代纯云端记账 App |
| 实训营学员 | 展示 AI 工程化能力 | 项目演示与路演 |

### 1.4 产品愿景
成为每个人电脑里的“私人财务管家”，用 AI 让记账从枯燥的记录变成有温度的财务对话。

### 1.5 产品定位边界
本文档定义的是 **MVP（最小可行产品）**，核心验证假设：**“用户是否愿意通过 AI 对话来理解自己的消费习惯”**。明确声明：
- 本产品不是专业财务软件，不提供税务/投资建议
- AI 分析仅为参考，不承担个人财务决策责任
- 所有账单数据仅存储在浏览器本地，开发者无法访问
- 基础记账、统计图表功能完全离线可用；AI 分析与对话功能需联网（仅传输账单摘要数据）
- DeepSeek API 调用可能产生费用（用户需自行提供 API Key）

---

## 2. 用户故事与核心场景

### 2.1 核心用户故事
- 作为一名 **刚工作的毕业生**，我想**简单记录每日开销，并让 AI 告诉我上个月餐饮是否超标**，以便调整消费习惯。
- 作为一名 **自由设计师**，我想**分类记录项目收入和日常支出，并问 AI “这个季度哪个月盈利能力最强”**，以便规划现金流。
- 作为一名 **实训营学员**，我想**演示从记账、图表生成到 AI 对话的完整人机协同流程**，展示提示词工程和智能体设计能力。

### 2.2 核心使用流程

```
┌──────────────────────────────────────────────────────┐
│ Step 1: 打开应用（浏览器本地）                          │
│ 无需注册登录，直接进入记账主界面                           │
├──────────────────────────────────────────────────────┤
│ Step 2: 快速记账                                       │
│ 填写金额、类别、日期 → 保存至 localStorage                │
├──────────────────────────────────────────────────────┤
│ Step 3: 查看统计图表                                    │
│ 自动生成饼图（类别占比）、折线图（近7天趋势）              │
├──────────────────────────────────────────────────────┤
│ Step 4: 一键 AI 分析                                    │
│ 点击按钮 → 构建 Prompt（含账单摘要）→ 调用 DeepSeek API  │
│ → 流式返回分析报告（消费结构+问题+建议）                   │
├──────────────────────────────────────────────────────┤
│ Step 5: 对话式财务顾问                                   │
│ 打开聊天窗口，自由提问：“我如何每月省下500元？”           │
│ AI 结合账单上下文给出具体策略，支持多轮追问               │
├──────────────────────────────────────────────────────┤
│ Step 6: 迭代（可选）                                     │
│ 继续记账 → 再次分析 → 对比本月与上月AI建议的变化          │
└──────────────────────────────────────────────────────┘
```

### 2.3 典型使用场景

**场景 A：月底消费复盘**
- 用户小张每月中旬记账，月底查看统计：餐饮占比 45%
- 点击“AI 分析本月账单”，AI 提示：“你的餐饮支出高于建议上限（30%），其中外卖占 70%。建议每周自炊 3 次，预计可节省 400 元。”
- 小张追问：“有没有不花时间又能省钱的招？” AI 给出备餐、食堂等建议。

**场景 B：实训营路演**
- 演示者预先存入 20 条模拟账单
- 展示图表自动刷新 → 点击 AI 分析，流式输出报告（打字机效果）
- 打开聊天窗口，演示多轮对话：“我这个月买衣服太多，下个月预算怎么设？”
- AI 根据历史购物均值建议预算上限，体现智能体工作流

---

## 3. 功能需求

### 3.1 功能全景

```
AI 个人记账与消费分析助手 MVP
├── P0 核心闭环（Sprint 1，8小时）
│   ├── 收支记账（金额、类别、日期、备注）
│   ├── 账单列表与编辑/删除
│   ├── 月度统计（总收入/支出/结余）
│   ├── 可视化图表（类别饼图、近7天折线图）
│   ├── AI 一键分析（消费洞察 + 建议）
│   ├── 对话式财务顾问（多轮上下文）
│   └── 数据本地持久化（localStorage）
│
├── P1 体验增强（后续迭代）
│   ├── AI 自动归类（输入备注，模型推断类别）
│   ├── 预算预警智能体（超预算时主动推送提醒）
│   ├── 数据导出（CSV/图片）
│   ├── 主题切换
│   └── 消费记录搜索
│
└── 本期明确不做
    ├── 多用户/云同步
    ├── 银行账单自动导入
    ├── 移动端 App
    ├── 语音记账
    └── 投资/理财建议
```

### 3.2 P0 功能详述

**F1：收支记账**
| 属性 | 描述 |
|------|------|
| 输入字段 | 金额（数字，必填）、类型（支出/收入）、类别（餐饮/交通/购物/娱乐/居住/医疗/其他）、日期（默认当天）、备注（可选） |
| 交互方式 | 点击“记一笔”弹出对话框或展开内嵌表单，快速填写 |
| 快捷操作 | 预设常用类别图标，一键选择；金额支持小数点 |
| 验收标准 | 提交后账单列表即时刷新，数据写入 localStorage，刷新页面不丢失 |

**F2：账单列表与管理**
| 属性 | 描述 |
|------|------|
| 展示方式 | 按日期倒序列表，每条显示日期、类别标签、金额（支出红，收入绿）、备注 |
| 编辑/删除 | 点击编辑图标，表单回填，修改后保存；删除前弹出确认框 |
| 月度总览 | 顶部卡片显示本月总收入、总支出、结余 |
| 验收标准 | 增删改操作即时反映在列表和统计中，无任何卡顿 |

**F3：可视化统计图表**
| 属性 | 描述 |
|------|------|
| 图表类型 | 类别支出占比饼图（环形）、近 7 天每日支出折线图 |
| 数据来源 | 实时从 localStorage 计算，不依赖后端 |
| 空状态 | 无数据时显示引导文案“记一笔吧~” |
| 验收标准 | 图表数据与账单列表完全一致，切换月份图表动态更新 |

**F4：AI 一键分析（核心智能体）**
| 属性 | 描述 |
|------|------|
| 触发方式 | 统计区域“AI 分析本月账单”按钮 |
| 处理流程 | ① 前端生成当月账单摘要（总收入/支出/结余、类别占比、日均支出、最大单笔等） ② 嵌入 System Prompt 模板 ③ 调用 DeepSeek Chat Completions API（`/v1/chat/completions`）流式请求 ④ 实时展示返回文本 |
| Prompt 策略 | 角色设定：私人财务管家；输出要求：口语化，点明消费结构、对比健康比例、给出 2-3 条具体省钱建议，200 字内 |
| 异常处理 | 网络错误、API 密钥无效、额度不足、服务不可用等，前端均给出明确中文提示 |
| 验收标准 | 点击后 3 秒内开始输出文字，报告包含结构化要点，无明显幻觉 |

**F5：对话式财务顾问**
| 属性 | 描述 |
|------|------|
| 入口 | 右下角悬浮聊天图标，点击展开半屏聊天窗口 |
| 多轮对话 | 保留当前会话最近 6 轮对话；用户提问时，将历史 + 本月摘要作为上下文 |
| 快捷提问 | 预设“我这个月钱花哪了？”“怎么减少咖啡开销？”“帮我设个下月预算” |
| 上下文管理 | 仅发送当月账单摘要 + 最近 6 轮对话，不发送完整流水；所有请求通过 HTTPS 加密传输 |
| 验收标准 | 回答与账单数据相关，多轮对话上下文连贯，支持追问；单次请求响应时间 < 5 秒 |

**F6：数据持久化**
| 属性 | 描述 |
|------|------|
| 存储方式 | 浏览器 localStorage，key 为 `finance_records` |
| 数据结构 | 数组，每项包含 id, type, category, amount, date, note |
| 初始化 | 应用首次加载时检查并初始化空数组，无服务端依赖 |

---

## 4. 技术架构

### 4.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                 用户浏览器 (Vue 3 + Vite)                      │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌───────────────┐ │
│  │ 记账表单 │  │ 账单列表  │  │ 统计图表  │  │ AI 分析面板   │ │
│  │         │  │          │  │ (ECharts) │  │ (流式输出)    │ │
│  └────┬────┘  └────┬─────┘  └─────┬────┘  └───────┬───────┘ │
│       │            │              │               │          │
│  ┌────┴────────────┴──────────────┴───────────────┴───────┐ │
│  │          前端状态管理 (Vue Reactivity)                   │ │
│  │  ┌─────────────────┐  ┌────────────────────────────┐   │ │
│  │  │  localStorage    │  │  AI Service               │   │ │
│  │  │  账单数据读写      │  │  (DeepSeek API 调用)       │   │ │
│  │  └─────────────────┘  │  构建Prompt + 流式解析      │   │ │
│  │                       └─────────────┬──────────────┘   │ │
│  └─────────────────────────────────────┼──────────────────┘ │
└────────────────────────────────────────┼────────────────────┘
                                         │ HTTPS
                                         │ https://api.deepseek.com/v1/chat/completions
┌────────────────────────────────────────┼────────────────────┐
│               DeepSeek 大模型 API（云端）                      │
│  ┌─────────────────────────────────────┴──────────────────┐ │
│  │                    deepseek-chat (V4)                    │ │
│  │          消费分析 · 建议生成 · 对话回答                  │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

**架构说明**：
- **前端**：Vue 3 负责全部 UI 与交互，`localStorage` 完成账单数据本地持久化。
- **AI Service 层**：封装 DeepSeek Chat Completions API，将匿名账单摘要构建为提示词，通过流式请求获取分析结果并实时渲染。
- **模型服务**：采用 DeepSeek 云端大模型（`deepseek-chat`），所有 AI 推理通过 HTTPS 加密传输，仅上传统计摘要，绝不包含完整流水。

### 4.2 技术选型

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| 前端框架 | Vue 3 (Composition API) + Vite | 实训营指定，响应式数据流 |
| UI 组件库 | Element Plus | 表单、对话框、消息提示开箱即用 |
| 图表库 | ECharts | 饼图、折线图配置灵活 |
| 本地存储 | localStorage | 零依赖，满足个人记账数据量 |
| 大模型服务 | DeepSeek (deepseek-chat) | 接入简单、性能好、中文理解强 |
| 流式请求 | fetch + ReadableStream | 实现打字机效果 |
| 开发工具 | VS Code + Claude Code | 实训营 AI IDE 推荐 |
| API 密钥存储 | 浏览器 localStorage | 本地保存，不上传任何服务器 |

### 4.3 核心数据流

```
用户记账 → Vue 响应式数据更新 → 写入 localStorage
                                 ↓
                          统计计算（纯前端） → 图表更新
                                 ↓
                      用户点击“AI分析”
                                 ↓
                      前端校验 API 密钥是否已配置
                                 ↓
        构造 DeepSeek 请求体（model, stream, messages）
                                 ↓
    fetch POST → https://api.deepseek.com/v1/chat/completions
                                 ↓
              逐块解析 SSE 响应 → 过滤 → 更新 UI 文本
```

---

## 5. 数据模型

### 5.1 核心数据结构（localStorage）

```typescript
// 单条记账记录
interface Transaction {
  id: string;            // UUID
  type: 'income' | 'expense';
  category: string;      // 如 “餐饮”, “交通”, “购物” ...
  amount: number;
  date: string;          // ISO 日期字符串，如 "2026-05-19"
  note: string;
}

// 完整存储结构
type FinanceData = Transaction[];
```

### 5.2 前端状态模型（运行时）

```typescript
// 筛选条件
interface Filter {
  month: string;   // "2026-05"
}

// AI 对话消息
interface ChatMessage {
  role: 'user' | 'assistant';
  content: string;
  timestamp: number;
}

// API 配置（存储在 localStorage）
interface ApiConfig {
  apiKey: string;
  model: string;   // 默认 "deepseek-chat"
}
```

### 5.3 Prompt 模板（核心）

```javascript
// 一键分析 Prompt
export const ANALYSIS_PROMPT = (summary) => `
你是一位细心又亲切的私人财务管家。
请根据以下用户本月消费摘要，用口语化、友好的语气给出分析：
1. 消费结构中的亮点或问题；
2. 与健康财务比例（餐饮20-30%，购物15-20%，娱乐5-10%）的对比；
3. 2-3条针对该用户的具体可行的省钱建议。
请控制在200字以内，不要使用Markdown。
消费摘要：${summary}
`;

// 对话顾问 System Prompt
export const CHAT_SYSTEM_PROMPT = `
你是用户的私人财务管家，可以基于提供的账单数据回答问题。
保持亲切、鼓励的语气，给出具体建议。如果问题超出财务范围，礼貌引导回正题。
`;
```

---

## 6. 非功能性需求

### 6.1 性能

| 指标 | 目标值 | 测量方法 |
|------|--------|----------|
| AI 分析首字延迟 | < 3 秒 | 从点击按钮到出现第一个字的时长 |
| 完整分析生成 | < 15 秒 | 流式输出结束时间 |
| 图表渲染 | < 500ms | ECharts 初始化到可交互 |
| 记账操作响应 | < 100ms | 提交到列表刷新 |

### 6.2 可用性
- 浏览器兼容：Chrome 90+, Edge 90+（仅桌面端）
- 界面语言：简体中文
- 空状态/错误状态均有引导
- 无任何登录注册，打开即用
- 首次使用提供简单的 API Key 配置引导

### 6.3 安全性 & 隐私

| 要求 | 实现方式 |
|------|----------|
| 数据本地存储 | 仅存于 localStorage，完整流水永不外传 |
| AI 请求隔离 | 仅向 DeepSeek 发送匿名统计摘要（无用户标识、无单笔交易详情）；所有请求通过 HTTPS 加密 |
| 无第三方依赖 | 不加载外部 CDN（除初始化 npm 包），仅向 `api.deepseek.com` 发起网络请求 |
| 用户知情权 | 顶部明确标注“所有数据存储在本机，AI 分析仅上传匿名统计摘要，DeepSeek 不保留您的请求数据” |
| 密钥安全 | API 密钥存储在浏览器 localStorage 中，仅在当前设备使用，不上传至任何服务器 |

### 6.4 可靠性
- 应用启动时自动检测 API 密钥是否已配置，未配置则引导用户输入
- 网络断开时，自动禁用 AI 功能，提示 “网络已断开，AI 功能不可用”
- API 请求失败时，自动重试 1 次；重试失败则显示对应错误提示（如密钥错误、额度不足等）
- 异常输入（如金额负数）前端校验拦截；无后端，无单点故障

---

## 7. 开发计划与里程碑（一天 Sprint）

| 时间段 | 模块 | 关键任务 | 产出 |
|--------|------|----------|------|
| 0.5h | 项目初始化 | Vite + Vue3 + ElementPlus + ECharts 脚手架搭建，配置 DeepSeek API 服务层 | 可运行空项目 |
| 1.5h | 核心记账 | 记账表单、账单列表、localStorage CRUD | 数据流闭环 |
| 1h | 可视化统计 | 类别饼图、近7天折线图，绑定真实数据 | 静态图表可用 |
| 2h | AI 分析 | 撰写分析 Prompt，对接 DeepSeek API 流式输出，UI 展示，API Key 配置界面 | 一键分析可用 |
| 1.5h | 对话顾问 | 聊天 UI，多轮上下文管理，快捷问题 | 多轮对话可用 |
| 1h | UI 打磨与异常处理 | 加载态、错误提示、空状态、响应式微调 | 产品体验完整 |
| 0.5h | 路演准备 | 准备演示数据集（20条模拟账单），编写 README | 演示就绪 |

### 7.1 路演演示脚本（5分钟）
1. **项目介绍（30秒）**：本地存储 + AI 云端分析的记账助手，解决隐私和传统记账缺乏洞察的痛点。
2. **核心功能演示（3分钟）**：
   - 快速记账 → 账单列表 → 统计图表（展示前端数据流）
   - 一键 AI 分析（展示流式输出和消费洞察）
   - 多轮对话追问（展示上下文管理能力）
3. **技术亮点（1分钟）**：Vue3 组件化、DeepSeek API 集成、提示词工程、人机协同开发。
4. **总结（30秒）**：符合 MVP 定位，覆盖实训营核心知识点。

---

## 8. 成功指标（MVP 验收标准）

| 编号 | 验收项 | 标准 |
|------|--------|------|
| 1 | 记账功能 | 可增删改查记录，刷新后数据不丢失 |
| 2 | 统计图表 | 饼图、折线图根据数据动态渲染，无数据时显示空状态 |
| 3 | AI 一键分析 | 3秒内开始输出，15秒内生成自然语言报告，包含消费结构和建议 |
| 4 | 对话式顾问 | 多轮对话上下文一致，回答贴合账单数据 |
| 5 | 本地运行与联网 | 基础记账图表功能无需网络；AI 功能需联网且正常调用 DeepSeek API |
| 6 | 隐私合规 | 无完整流水外传，仅发送匿名摘要到 `api.deepseek.com` |
| 7 | 实训展示 | 可流畅演示“记账 → 图表 → AI 分析 → 追问”闭环 |

---

## 9. 风险与边界

### 9.1 技术风险

| 风险 | 影响 | 缓解策略 |
|------|------|----------|
| DeepSeek API 不可用 | AI 功能完全失效 | 前端明确提示网络或服务异常，引导用户检查网络或稍后重试 |
| API 密钥泄露或额度用尽 | 无法调用 AI | 密钥存储在本地，用户自行管理；额度用尽时给出充值/更换指引 |
| 模型输出不可控 | 分析跑题或幻觉 | Prompt 约束输出结构；前端简单过滤明显无关内容 |
| 网络延迟导致响应慢 | 用户体验下降 | 使用流式输出，UI 显示打字机效果；超时重试机制 |
| localStorage 容量不足 | 无法保存更多记录 | MVP 阶段数据量极小，风险低；未来可迁移至 IndexedDB |

### 9.2 边界与不做

| 项目 | 原因 |
|------|------|
| 云同步/多设备 | 保持本地优先，符合隐私定位 |
| 语音记账 | MVP 聚焦文本交互 |
| 发票扫描 | 复杂度高，偏离实训目标 |
| 预算自动扣款 | 非财务工具，仅为分析助手 |
| 社交分享 | 隐私优先，不设计分享功能 |

### 9.3 AI 模型局限性声明
产品界面将提示：“AI 分析结果由 DeepSeek 大模型生成，可能存在偏差，仅供个人参考，不构成专业财务建议。”

---

## 10. 附录：AI 编码最佳实践与提示词工程

### 10.1 本 PRD 如何支撑 AI 编码代理
参照“AI 审稿人”样例，本文档的每一章节都为 AI 编码助手提供精确上下文：

| 开发任务 | AI 所需上下文 | 对应章节 |
|----------|---------------|----------|
| 项目初始化 | 技术栈、目录结构 | 第 4 节：技术架构 |
| 数据层实现 | 数据结构、持久化方式 | 第 5 节：数据模型 |
| 组件开发 | 交互细节、验收标准 | 第 3 节：功能详述 |
| AI 服务对接 | API 格式、Prompt 模板、流式解析 | 第 4.3 节 + 第 5.3 节 |
| 异常处理 | 错误场景、降级策略 | 第 6.4 节 + 第 9 节 |

### 10.2 提示词迭代记录模板
建议小组在开发过程中维护以下表格，作为实训营“提示词工程”模块的产出：

| 版本 | 日期 | 修改内容 | 效果对比 |
|------|------|----------|----------|
| v1 | 5.19 | 初始分析 Prompt，无角色设定 | 输出较机械 |
| v2 | 5.19 | 增加“私人财务管家”角色和语气要求 | 更亲切，建议更具体 |
| v3 | 5.19 | 追加“与健康比例对比”指令 | 分析有了量化参考 |

---

**文档结束**  
*本文档将作为小组开发期的唯一需求依据，所有功能取舍以 P0 范围为准。*
```